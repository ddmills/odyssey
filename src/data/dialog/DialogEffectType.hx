package data.dialog;

enum abstract DialogEffectType(String) to String from String
{
	var EFFECT_RELATION = "EFFECT_RELATION";
	var EFFECT_HEAL = "EFFECT_HEAL";
	var EFFECT_FLAG = "EFFECT_FLAG";
	var EFFECT_AUDIO = "EFFECT_AUDIO";
}
